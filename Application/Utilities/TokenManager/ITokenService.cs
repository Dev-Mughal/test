using Common.Models;
using Domain;

namespace Application.Utilities.TokenManager
{
    public interface ITokenService
    {
        // ?? Business User ????????????????????????????????????????????????????
        TokenResponse GenerateTokens(BusinessUser user, int businessId, bool rememberMe = false);
        TokenResponse GenerateTokens(AuthorizedUserDto user, bool rememberMe = false);
        Task SaveRefreshTokenAsync(long userId, string refreshToken, bool rememberMe = false);
        Task RevokeRefreshTokenAsync(long userId);
        bool IsRememberMeSession(DateTime? refreshTokenExpiryTime);

        // ?? Customer ?????????????????????????????????????????????????????????
        TokenResponse GenerateCustomerTokens(Customer customer, bool rememberMe = false);
        Task SaveCustomerRefreshTokenAsync(long customerId, string refreshToken, bool rememberMe = false);
        Task RevokeCustomerRefreshTokenAsync(long customerId);
    }
}

